# my-project-name

Java AI sandbox experiment.

## Prerequisites

- JDK 21+
- Maven 3.9+ (or generate `mvnw` in this folder)

## Run

```bash
mvn -q compile exec:java
```

## Package

```bash
mvn -q package
java -jar target/my-project-name-0.1.0-SNAPSHOT.jar
```
