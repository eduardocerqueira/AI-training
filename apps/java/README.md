# Java apps

Spikes for JVM ecosystems, Spring-style APIs, or vendor SDKs that target Java first.

## New project

```bash
cp -r _template my-project-name
cd my-project-name
mvn -q compile exec:java
```

Requires JDK 21+ and Maven 3.9+.
