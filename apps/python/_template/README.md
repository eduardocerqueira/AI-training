# my-project-name

> Replace this title and describe what you are learning or proving.

## Prerequisites

- Python 3.11+
- Optional: `OPENAI_API_KEY` or other keys (see `.env.example`)

## Setup

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -e ".[dev]"
cp .env.example .env        # then edit .env
```

## Run

```bash
python -m my_project_name
```

## Notes

- Link to course or docs you are following.
- List known limitations or next steps.
