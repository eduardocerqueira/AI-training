# TypeScript apps

Good fit for MCP servers, LangChain.js, and typed Node tooling.

## New project

```bash
cp -r _template my-project-name
cd my-project-name
npm install
cp .env.example .env
npm run dev
```
