# Node (JavaScript) apps

Use when you want plain JavaScript without TypeScript, or quick scripts against provider SDKs.

## New project

```bash
cp -r _template my-project-name
cd my-project-name
npm install
node src/index.js
```
